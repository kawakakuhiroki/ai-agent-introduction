# AGENTS.md

> このファイルは、本プロジェクトで作業するすべての AI エージェント（Claude Code、Codex など）が従う共通ルールを定義する。

## 基本方針

- AI エージェントは、ユーザーの明示的な許可なくファイルを編集してはいけません。
- ファイル編集、削除、移動、リネーム、生成を伴う変更は、必ず事前にユーザーへ内容を説明し、許可を得てから実施してください。
- 既存ファイルを直接上書きせず、バージョン付き別ファイルとして出力する。
  - 命名規則：`ファイル名_vN.拡張子`（例：`検討資料_v6.xlsx`、`AGENTS_v3.md`）
  - 作業完了時に「最新版は `ファイル名_vN.拡張子` です」とユーザーへ明示すること。

## 導入環境

AIエージェントを導入する環境について記載する。

- OS：Windows（WSLは使用しない。ネイティブWindowsのみ）
- ネットワーク：イントラネット環境。外部接続はプロキシ経由
- パス区切り：バックスラッシュ `\`（スクリプト内は `/` も可）
- 文字コード：UTF-8（BOMなし）
- 改行コード：LF（Git管理下）
- シェル：PowerShell（特に指定なき場合）

## 調査・確認の方針

- 事実確認は、各製品の公式ドキュメントおよび公開情報を一次情報として行う。
- ベンダーへの個別問い合わせ（ベンダー確認）は行わない。
- 公開情報で確定できない事項は以下の3区分を使い分け、推測で断定しない。
  - **要確認**：公開情報で確認できなかった（調査の余地あり）
  - **要PoC確認**：公開情報では不明で、実機検証が必要
  - **不明**：公開情報がなく判断不可（現時点での到達限界）
- 特定の製品を推さずに公平に比較する。

## Excel資料作成・更新時のツール利用

- `.xlsx` ファイルの評価、分析、作成、更新を行う場合は、利用中のエージェントが備えるスプレッドシート操作機能を用いる（例: Codex の `@spreadsheets`、Claude Code の xlsx スキル）。
- 既存Excel資料の見た目、改ページ、セル幅、折り返し、レイアウト崩れを確認する必要がある場合は、実際の Excel で開いて確認できる手段を用いる（例: Codex の `@Microsoft Excel`）。その手段が利用できない場合は、確認できない旨を伝えた上で代替手段で対応する。
- ツールの種類にかかわらず、Excel資料を作成・更新する場合は、既存のシート構成、見出し位置、セル配置、列幅、行高、書式をできるだけ維持する。

## Excel資料の新規シート作成

- `検討資料.xlsx` に新規シートを追加する場合は、原則として既存の `テンプレ` シートをコピーして作成する。
- 表形式など、テンプレにそぐわないものは一から作成してOK。
- コピー後に、シート名、題名、見出し、本文を目的に応じて差し替える。
- `テンプレ` シート自体は変更しない。
- 大幅な構成変更が必要な場合は、事前にユーザーへ説明し、許可を得てから実施する。

## サブエージェント活用ルール

> **現時点では未使用。** 現在の検討資料の規模（3製品・5カテゴリ）は Claude Code 単体で対応する。
> 並列処理が必要になったタイミングで設計・有効化する。

<!--
## （将来検討）サブエージェントの観点案

1. 製品調査
2. セキュリティ・データ取り扱い
3. ネットワーク・接続方式
4. PoC設計
5. 上司向け資料編集
6. 批判的レビュー
-->

## 参照禁止フォルダ

以下のフォルダ配下のファイルは読取・編集・出力先として使用しない。

- `work/`（作業中途ファイル・下書き・実験用。誤って成果物に混入させないため）

※パスはプロジェクトルートからの相対パス。
